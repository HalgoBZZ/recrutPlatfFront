import { OffreCompetencePipePipe } from './offre-competence-pipe.pipe';

describe('OffreCompetencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new OffreCompetencePipePipe();
    expect(pipe).toBeTruthy();
  });
});
